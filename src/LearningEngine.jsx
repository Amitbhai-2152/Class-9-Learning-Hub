import React,{useEffect,useState} from 'react';
import {markStageComplete} from './engines/progress/progressStore';
import {chapterEnhancements} from './chapterEnhancements';
import {chapter1Engagement} from './chapter1Engagement';
import {MathChapterVisual} from './MathChapterVisual';
import './engine.css';
import './chapter1Engagement.css';

const typeLabel={intro:'अध्याय की शुरुआत',concept:'अवधारणा',example:'समझें • उदाहरण',check:'त्वरित जाँच'};

const hindiReplacements=[
  [/input-output/gi,'मान-प्रवेश और परिणाम'],[/input/gi,'मान'],[/output/gi,'परिणाम'],[/polynomial/gi,'बहुपद'],[/polynomials/gi,'बहुपद'],[/factorisation/gi,'गुणनखंडन'],[/factorization/gi,'गुणनखंडन'],[/factor theorem/gi,'गुणनखंड प्रमेय'],[/factor/gi,'गुणनखंड'],[/factors/gi,'गुणनखंड'],[/zeroes/gi,'शून्यक'],[/zero/gi,'शून्यक'],[/degree/gi,'घात'],[/coefficient/gi,'गुणांक'],[/constant term/gi,'अचर पद'],[/constant polynomial/gi,'अचर बहुपद'],[/constant/gi,'अचर'],[/linear polynomial/gi,'रैखिक बहुपद'],[/linear/gi,'रैखिक'],[/quadratic/gi,'द्विघात'],[/cubic/gi,'घन'],[/expression/gi,'व्यंजक'],[/equation/gi,'समीकरण'],[/graph/gi,'आलेख'],[/slope-intercept form/gi,'ढाल-अवरोध रूप'],[/slope/gi,'ढाल'],[/y-intercept/gi,'y-अवरोध'],[/Cartesian plane/gi,'कार्तीय तल'],[/ordered pair/gi,'क्रमित युग्म'],[/coordinate plane/gi,'निर्देशांक तल'],[/midpoint/gi,'मध्यबिंदु'],[/distance formula/gi,'दूरी का सूत्र'],[/point/gi,'बिंदु'],[/points/gi,'बिंदु'],[/quadrants/gi,'चतुर्थांश'],[/quadrant/gi,'चतुर्थांश'],[/axis/gi,'अक्ष'],[/axes/gi,'अक्ष'],[/origin/gi,'मूलबिंदु'],[/right triangle/gi,'समकोण त्रिभुज'],[/theorem/gi,'प्रमेय'],[/axiom/gi,'स्वयंसिद्ध'],[/postulate/gi,'अभिगृहीत'],[/proof/gi,'प्रमाण'],[/proofs/gi,'प्रमाण'],[/concept/gi,'अवधारणा'],[/visual/gi,'दृश्य'],[/example/gi,'उदाहरण'],[/practice/gi,'अभ्यास'],[/challenge/gi,'चुनौती'],[/test/gi,'परीक्षा'],[/quick check/gi,'त्वरित जाँच'],[/learning steps/gi,'सीखने के चरण'],[/real life/gi,'दैनिक जीवन'],[/real-life/gi,'दैनिक जीवन'],[/machine/gi,'यंत्र'],[/pattern/gi,'प्रतिरूप'],[/growth/gi,'वृद्धि'],[/decay/gi,'कमी'],[/fixed charge/gi,'स्थिर शुल्क'],[/per unit/gi,'प्रति इकाई'],[/unit/gi,'इकाई'],[/summary/gi,'सारांश'],[/takeaway/gi,'मुख्य सीख'],[/reading time/gi,'पढ़ने का समय']
];
function hiText(value){if(typeof value!=='string')return value;return hindiReplacements.reduce((text,[re,repl])=>text.replace(re,repl),value);}
function hiList(list){return Array.isArray(list)?list.map(hiText):list;}

function NumberLine({items=[]}){ return <div className="visual-canvas numberline-canvas"><div className="numberline-track"><span className="numberline-axis"/><span className="numberline-arrow left"/><span className="numberline-arrow right"/><div className="numberline-points">{hiList(items).map((x,i)=><div className="number-point" key={`${x}-${i}`}><span className="point-dot"/><strong>{x}</strong></div>)}</div></div></div>; }
function FractionVisual({items=[]}){ return <div className="visual-canvas fraction-canvas"><div className="fraction-core"><span className="fraction-symbol">p</span><span className="fraction-line"/><span className="fraction-symbol">q</span></div><div className="fraction-labels">{hiList(items).map((x,i)=><span key={`${x}-${i}`}>{x}</span>)}</div></div>; }
function FlowVisual({items=[]}){ return <div className="visual-canvas flow-canvas">{hiList(items).map((x,i)=><React.Fragment key={`${x}-${i}`}><div className="flow-node"><span>{i+1}</span><strong>{x}</strong></div>{i<items.length-1&&<span className="flow-arrow">→</span>}</React.Fragment>)}</div>; }
function StepsVisual({items=[]}){ return <div className="visual-canvas steps-canvas">{hiList(items).map((x,i)=><div className="step-card" key={`${x}-${i}`}><span>{i+1}</span><strong>{x}</strong></div>)}</div>; }
function CompareVisual({items=[]}){ return <div className="visual-canvas compare-canvas">{hiList(items).map((x,i)=><div className="compare-row" key={`${x}-${i}`}><span>{String.fromCharCode(65+i)}</span><strong>{x}</strong></div>)}</div>; }
function EquationVisual({items=[]}){ return <div className="visual-canvas equation-canvas">{hiList(items).map((x,i)=><div className="equation-chip" key={`${x}-${i}`}><span>{i+1}</span><strong>{x}</strong></div>)}</div>; }
function NestedVisual({items=[]}){ return <div className="visual-canvas nested-canvas">{hiList(items).map((x,i)=><div className={`nested-row nested-${Math.min(i,3)}`} key={`${x}-${i}`}><span>{i+1}</span><strong>{x}</strong></div>)}</div>; }
function LessonVisual({visual}){ if(!visual)return null; const kind=visual.type||'flow'; const title=hiText(visual.title||'अवधारणा का दृश्य'); const renderer={numberline:<NumberLine items={visual.items}/>,fraction:<FractionVisual items={visual.items}/>,flow:<FlowVisual items={visual.items}/>,steps:<StepsVisual items={visual.items}/>,compare:<CompareVisual items={visual.items}/>,equation:<EquationVisual items={visual.items}/>,nested:<NestedVisual items={visual.items}/>} [kind] || <FlowVisual items={visual.items}/>; return <div className={`lesson-visual visual-${kind}`}><div className="visual-title"><span>✨</span><div><strong>{title}</strong><small>देखें और अवधारणा को याद रखें</small></div></div>{renderer}</div>; }
function EngagementCard({content}){ if(!content)return null; return <div className="engagement-card"><div className="engagement-block"><span className="engagement-label">आज का सवाल</span><strong>{hiText(content.hook)}</strong></div><div className="engagement-grid"><div><span className="engagement-label">क्यों सीख रहे हैं?</span><p>{hiText(content.why)}</p></div><div><span className="engagement-label">🤔 पहले सोचें</span><p>{hiText(content.think)}</p></div></div></div>; }

export function LearningEngine({subject,chapter,data,onBack,addXp,finishSession}){
  const lessons=data?.lessons||[];
  const chapterId=`${subject?.name||'विषय'}::${chapter||'अध्याय'}`;
  const enhancement=chapterEnhancements[`${subject?.name}|${chapter}`]||{};
  const [index,setIndex]=useState(0),[answers,setAnswers]=useState({}),[completed,setCompleted]=useState(false),[startedAt]=useState(Date.now());
  useEffect(()=>{try{const raw=localStorage.getItem(`class9-learning:${chapterId}`);if(raw){const state=JSON.parse(raw);if(Number.isInteger(state.index))setIndex(Math.min(Math.max(state.index,0),Math.max(lessons.length-1,0)));if(state.answers)setAnswers(state.answers)}}catch{}},[chapterId,lessons.length]);
  useEffect(()=>{try{localStorage.setItem(`class9-learning:${chapterId}`,JSON.stringify({index,answers}))}catch{}},[chapterId,index,answers]);
  const item=lessons[index]; const percent=lessons.length?Math.round(((index+1)/lessons.length)*100):0; const selected=answers[index];
  const checkAnswer=value=>{if(selected!==undefined)return;setAnswers(a=>({...a,[index]:value}))};
  const correctCount=Object.entries(answers).reduce((n,[i,v])=>n+(String(v)===String(lessons[i]?.answer)?1:0),0);
  const goNext=()=>{if(index<lessons.length-1){setIndex(i=>i+1);return;}if(item.question&&selected===undefined)return;if(!completed){setCompleted(true);markStageComplete(chapterId,'learn');addXp?.(20);finishSession?.({subject:subject?.name,chapter,mode:'learn',completed:true,attempted:lessons.filter(x=>x.question).length,correct:correctCount,at:Date.now(),durationSeconds:Math.round((Date.now()-startedAt)/1000)});return;}onBack()};
  if(!item)return <main className="page"><header className="page-header"><button className="pressable" onClick={onBack}>← अध्याय</button><h1>📖 सीखें</h1></header><section className="page-content"><div className="empty-review"><span>📚</span><h2>अभी सामग्री उपलब्ध नहीं है</h2><p>इस अध्याय की सीखने की सामग्री जुड़ने पर यहाँ दिखाई देगी।</p></div></section></main>;
  if(completed)return <main className="page lesson-page"><header className="page-header success-header"><button className="pressable" onClick={onBack}>← अध्याय</button><div className="badge">सीखना पूरा</div><h1>🎉 अध्याय पूरा हुआ</h1><p>आपने पूरा सीखने का मार्ग पूरा कर लिया। अब अभ्यास से इसे मजबूत करें।</p></header><section className="page-content"><div className="completion-grid"><div className="result-card completion-hero"><div className="completion-trophy">🏆</div><div className="result-score">+20<small> XP</small></div><h2>बहुत बढ़िया! 📖</h2><p>त्वरित जाँच में <strong>{correctCount}</strong> उत्तर सही रहे।</p><div className="result-actions"><button className="secondary-btn pressable" onClick={()=>{setCompleted(false);setIndex(0)}}>फिर से पढ़ें</button><button className="primary-btn pressable" onClick={onBack}>अध्याय पर जाएँ →</button></div></div>{(enhancement.summary?.length||enhancement.mistakes?.length||enhancement.takeaway)&&<div className="summary-card"><span className="lesson-type">अध्याय पुनरावलोकन</span><h2>याद रखने योग्य बातें</h2>{enhancement.summary?.length>0&&<><h3>मुख्य बिंदु</h3><ul>{hiList(enhancement.summary).map((x,i)=><li key={`${x}-${i}`}>{x}</li>)}</ul></>}{enhancement.mistakes?.length>0&&<><h3>इन गलतियों से बचें</h3><ul>{hiList(enhancement.mistakes).map((x,i)=><li key={`${x}-${i}`}>{x}</li>)}</ul></>}{enhancement.takeaway&&<div className="takeaway"><strong>🎯 मुख्य सीख</strong><p>{hiText(enhancement.takeaway)}</p></div>}</div>}</div></section></main>;
  const currentEngagement=chapter==='संख्या पद्धति'?chapter1Engagement['संख्या पद्धति']?.[index]:null;
  return <main className="page lesson-page"><header className="page-header lesson-header"><button className="pressable" onClick={onBack}>← अध्याय</button><div className="lesson-top"><span className="badge">{hiText(subject?.name)} • {hiText(chapter)}</span><span className="lesson-counter">{index+1} / {lessons.length}</span></div><div className="lesson-title-row"><div><h1>📖 सीखें</h1><p>अवधारणा समझें → दृश्य देखें → उदाहरण देखें → फिर जाँच करें।</p></div><div className="lesson-progress-ring"><span>{percent}%</span></div></div><div className="lesson-progress"><span style={{width:`${percent}%`}}/></div></header><section className="page-content"><div className="lesson-layout"><aside className="lesson-outline"><div className="outline-head"><div><span className="lesson-type">इस अध्याय में</span><strong>{lessons.length} सीखने के चरण</strong></div><span>{percent}%</span></div><div className="outline-list">{lessons.map((l,i)=><button className={`outline-item pressable ${i===index?'active':''} ${answers[i]!==undefined?'answered':''}`} key={`${l.title}-${i}`} onClick={()=>setIndex(i)}><span className="outline-index">{answers[i]!==undefined?'✓':i+1}</span><span className="outline-copy"><strong>{hiText(l.title)}</strong><small>{typeLabel[l.type]||'पाठ'}</small></span><span className="outline-arrow">›</span></button>)}</div></aside><article className="lesson-card">{subject?.name==='गणित'&&index===0&&<MathChapterVisual chapter={chapter}/>}<div className="lesson-card-top"><span className={`lesson-type type-${item.type}`}>{typeLabel[item.type]||'पाठ'}</span><span className="reading-time">⏱ 2–3 मिनट</span></div><h2>{hiText(item.title)}</h2><EngagementCard content={currentEngagement}/><p className="lesson-body">{hiText(item.body)}</p>{item.visual&&<LessonVisual visual={item.visual}/>} {item.points&&<div className="lesson-points-wrap"><div className="mini-heading">✅ याद रखें</div><ul className="lesson-points">{hiList(item.points).map((p,i)=><li key={`${p}-${i}`}>{p}</li>)}</ul></div>}{item.example&&<div className="lesson-example"><div className="mini-heading">💡 उदाहरण</div><p>{hiText(item.example)}</p></div>}{enhancement.examples?.[index]&&<div className="worked-example"><div className="mini-heading">🧩 चरणबद्ध उदाहरण</div><h3>{hiText(enhancement.examples[index].title)}</h3><ol>{hiList(enhancement.examples[index].steps).map((s,i)=><li key={`${s}-${i}`}>{s}</li>)}</ol></div>}{item.question&&<div className="quick-check"><div className="quick-check-head"><span className="mini-heading">🎯 त्वरित जाँच</span><small>पहले सोचें, फिर उत्तर चुनें</small></div><strong className="check-question">{hiText(item.question)}</strong><div className="check-options-grid">{item.options.map((option,i)=>{const isSelected=selected!==undefined&&String(selected)===String(i);const isCorrect=selected!==undefined&&String(item.answer)===String(i);return <button disabled={selected!==undefined} className={`check-option pressable ${isSelected?'selected':''} ${isCorrect?'correct':''}`} key={option} onClick={()=>checkAnswer(i)}><span>{String.fromCharCode(65+i)}</span>{hiText(option)}{isCorrect&&selected!==undefined?<b>✓</b>:null}</button>})}</div>{selected!==undefined&&<div className={`check-feedback ${String(selected)===String(item.answer)?'good':'bad'}`}><strong>{String(selected)===String(item.answer)?'✓ बिल्कुल सही!':'✗ उत्तर दोबारा समझें'}</strong><p>{hiText(item.explain||`सही उत्तर: ${item.options[item.answer]}`)}</p></div>}</div>}<div className="lesson-actions"><button className="secondary-btn pressable" onClick={onBack}>बाद में पढ़ूँगा</button><button className="primary-btn pressable" onClick={goNext}>{index===lessons.length-1?(item.question&&selected===undefined?'उत्तर चुनें':'अध्याय पूरा करें'):'आगे बढ़ें →'}</button></div></article></div></section></main>;
}
