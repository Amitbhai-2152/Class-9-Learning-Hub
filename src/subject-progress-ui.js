const SUBJECTS=[
 {id:'math',name:'गणित',total:15,icon:'∑'},
 {id:'science',name:'विज्ञान',total:15,icon:'⚗'},
 {id:'hindi',name:'हिन्दी',total:44,icon:'अ'},
 {id:'sanskrit',name:'संस्कृत',total:15,icon:'ॐ'},
 {id:'sst',name:'सामाजिक विज्ञान',total:33,icon:'◎'},
 {id:'english',name:'अंग्रेज़ी',total:46,icon:'A'},
 {id:'reasoning',name:'तर्कशक्ति',total:6,icon:'?'}
];
const STAGES=['learn','practice','challenge','test'];
const MODEL_KEY='class9-progress-v2';
const ANON_ID_KEY='class9-anonymous-student-id';
const EVENTS_KEY='class9-progress-events-v1';
const STYLE_ID='class9-subject-progress-styles';
const readJson=(key,fallback)=>{try{const raw=localStorage.getItem(key);return raw?JSON.parse(raw):fallback}catch{return fallback}};
const writeJson=(key,value)=>{try{localStorage.setItem(key,JSON.stringify(value));return true}catch{return false}};
const safeObj=value=>value&&typeof value==='object'&&!Array.isArray(value)?value:{};
const safeNum=(value,fallback=0)=>Number.isFinite(Number(value))?Number(value):fallback;
function getAnonStudentId(){let id='';try{id=localStorage.getItem(ANON_ID_KEY)||''}catch{}if(!id){try{id=crypto.randomUUID()}catch{id=`anon-${Date.now()}-${Math.random().toString(36).slice(2,10)}`};try{localStorage.setItem(ANON_ID_KEY,id)}catch{}}return id;}
function normalizeChapter(value){const source=safeObj(value);return {learn:Boolean(source.learn),practice:Boolean(source.practice),challenge:Boolean(source.challenge),test:Boolean(source.test),attempts:safeNum(source.attempts),correct:safeNum(source.correct),lastActivityAt:source.lastActivityAt||null};}
function classifySubject(name){if(!name)return null;const exact=SUBJECTS.find(s=>s.name===name);if(exact)return exact;const normalized=String(name).trim().toLowerCase();return SUBJECTS.find(s=>s.name.toLowerCase()===normalized)||null;}
function applyChapterStage(subjectBucket,chapter,stage,attempts=0,correct=0,at=null){if(!subjectBucket||!chapter||!STAGES.includes(stage))return subjectBucket;const chapters={...subjectBucket.chapters};const prev=normalizeChapter(chapters[chapter]);const timestamp=at||prev.lastActivityAt||null;chapters[chapter]={...prev,[stage]:true,attempts:Math.max(prev.attempts,attempts),correct:Math.max(prev.correct,correct),lastActivityAt:timestamp};return {...subjectBucket,chapters,updatedAt:timestamp};}
export function buildSubjectSnapshot(){
 const subjectState=Object.fromEntries(SUBJECTS.map(s=>[s.id,{...s,chapters:{},updatedAt:null}]));
 const touch=(subjectName,chapter,stage,attempts=0,correct=0,at=null)=>{const subject=classifySubject(subjectName);if(!subject||!chapter||!STAGES.includes(stage))return;subjectState[subject.id]=applyChapterStage(subjectState[subject.id],chapter,stage,attempts,correct,at)};
 const generic=readJson('class9-learning-progress',{});
 Object.entries(safeObj(generic)).forEach(([key,value])=>{if(key.startsWith('__')||!key.includes('::'))return;const parts=key.split('::');const subjectName=parts.shift();const chapter=parts.join('::');const row=normalizeChapter(value);STAGES.forEach(stage=>{if(row[stage])touch(subjectName,chapter,stage,row.attempts,row.correct,row.lastActivityAt)})});
 const sessions=[];const app=readJson('class9-progress',{});if(Array.isArray(app?.sessions))sessions.push(...app.sessions);const engine=readJson('class9-sessions',[]);if(Array.isArray(engine))sessions.push(...engine);
 sessions.forEach(session=>{if(!session?.completed||!session?.subject||!session?.chapter)return;const stage=STAGES.includes(session.mode)?session.mode:null;if(stage)touch(session.subject,session.chapter,stage,safeNum(session.attempted),safeNum(session.correct),session.at||null)});
 const hindi=readJson('class9-hindi-chapter-progress-v1',{});
 Object.entries(safeObj(hindi?.modes)).forEach(([id,modes])=>{Object.entries(safeObj(modes)).forEach(([stage,done])=>{if(done)touch('हिन्दी',id,stage,0,0,null)})});
 const subjects=Object.fromEntries(SUBJECTS.map(s=>{const source=subjectState[s.id];const entries=Object.entries(safeObj(source.chapters)).map(([chapter,value])=>[chapter,normalizeChapter(value)]);const completedStages=entries.reduce((n,[,v])=>n+STAGES.filter(stage=>v[stage]).length,0);const chapterStarted=entries.filter(([,v])=>STAGES.some(stage=>v[stage])).length;const fullyCompleted=entries.filter(([,v])=>STAGES.every(stage=>v[stage])).length;const attempts=entries.reduce((n,[,v])=>n+v.attempts,0);const correct=entries.reduce((n,[,v])=>n+v.correct,0);const percent=Math.round((completedStages/Math.max(1,s.total*STAGES.length))*100);return [s.id,{...s,chapters:Object.fromEntries(entries),completedStages,chapterStarted,fullyCompleted,attempts,correct,percent,updatedAt:source.updatedAt||null}]}));
 return {schemaVersion:2,studentId:getAnonStudentId(),updatedAt:new Date().toISOString(),subjects};
}
export function persistSubjectSnapshot(){const snapshot=buildSubjectSnapshot();writeJson(MODEL_KEY,snapshot);return snapshot;}
export function getSubjectProgress(){const stored=readJson(MODEL_KEY,null);if(stored?.schemaVersion===2&&stored?.subjects)return stored.subjects;return persistSubjectSnapshot().subjects;}
function collectEvents(){const value=readJson(EVENTS_KEY,[]);return Array.isArray(value)?value:[];}
export function appendProgressEvent(event){const safe={eventId:event?.eventId||`evt-${Date.now()}-${Math.random().toString(36).slice(2,9)}`,studentId:getAnonStudentId(),createdAt:event?.createdAt||new Date().toISOString(),...safeObj(event)};const next=[safe,...collectEvents()].slice(0,1000);writeJson(EVENTS_KEY,next);persistSubjectSnapshot();try{window.dispatchEvent(new CustomEvent('class9-progress-updated'))}catch{}return safe;}
function captureExistingSources(){const snapshot=buildSubjectSnapshot();const previous=readJson(MODEL_KEY,null);const same=previous?.schemaVersion===2&&JSON.stringify(previous.subjects)===JSON.stringify(snapshot.subjects);if(!same)writeJson(MODEL_KEY,snapshot);}
function installStyles(){if(document.getElementById(STYLE_ID))return;const style=document.createElement('style');style.id=STYLE_ID;style.textContent=`
 .spui-panel{margin-top:26px;padding:22px;border:1px solid #e4e8ef;border-radius:24px;background:linear-gradient(145deg,rgba(255,255,255,.96),rgba(248,250,255,.96));box-shadow:0 12px 34px rgba(22,32,48,.075);animation:spuiEnter .5s ease both}.spui-head{display:flex;justify-content:space-between;align-items:flex-start;gap:18px;margin-bottom:17px}.spui-eyebrow{display:block;color:#7a8496;font-size:.66rem;font-weight:950;letter-spacing:.15em}.spui-head h2{margin:6px 0 4px;font-size:1.55rem;letter-spacing:-.03em;color:#1c2739}.spui-head p{margin:0;color:#728095;font-size:.8rem;line-height:1.5}.spui-live{flex:0 0 auto;padding:7px 9px;border-radius:999px;background:#ecf9f4;border:1px solid #d4eee4;color:#208666;font-size:.62rem;font-weight:950;letter-spacing:.08em;box-shadow:0 4px 12px rgba(32,134,102,.06);animation:spuiPulse 2.6s ease-in-out infinite}.spui-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:11px}.spui-card{display:flex;align-items:center;gap:12px;width:100%;padding:14px;border:1px solid #e6eaf1;border-radius:17px;background:#fff;color:#1c2739;text-align:left;cursor:pointer;box-shadow:0 5px 16px rgba(20,30,45,.045);transition:transform .2s ease,box-shadow .2s ease,border-color .2s ease}.spui-card:hover{transform:translateY(-3px);border-color:#d7ddef;box-shadow:0 11px 24px rgba(20,30,45,.09)}.spui-card:active{transform:scale(.99)}.spui-icon{width:42px;height:42px;border-radius:13px;display:grid;place-items:center;background:#eef0ff;color:#5967e8;font-weight:950;font-size:20px;flex:0 0 auto;box-shadow:inset 0 1px 0 rgba(255,255,255,.8)}.spui-card:nth-child(2) .spui-icon{background:#eaf5ff;color:#1878c6}.spui-card:nth-child(3) .spui-icon{background:#fff1e6;color:#d56d22}.spui-card:nth-child(4) .spui-icon{background:#ffebf1;color:#c94b70}.spui-card:nth-child(5) .spui-icon{background:#eaf8f0;color:#198450}.spui-card:nth-child(6) .spui-icon{background:#e7f8ff;color:#157b9a}.spui-card:nth-child(7) .spui-icon{background:#fff5dc;color:#a26f08}.spui-main{min-width:0;flex:1}.spui-title{display:flex;justify-content:space-between;gap:10px;align-items:center}.spui-title strong{font-size:.94rem}.spui-title b{font-size:.84rem;color:#5664df}.spui-bar{display:block;height:7px;margin:8px 0 6px;background:#edf0f5;border-radius:999px;overflow:hidden}.spui-bar i{display:block;height:100%;width:0;border-radius:inherit;background:linear-gradient(90deg,#6271ea,#8b81f4,#54bfd5);box-shadow:0 0 12px rgba(91,108,226,.2);animation:spuiBar .8s cubic-bezier(.2,.75,.25,1) var(--spui-delay,0ms) both}.spui-meta{display:flex;justify-content:space-between;gap:8px;color:#7b8698;font-size:.66rem;line-height:1.3}.spui-meta em{font-style:normal;color:#27825f;font-weight:800}.spui-arrow{font-size:18px;color:#9aa3af;transition:transform .2s ease,color .2s ease}.spui-card:hover .spui-arrow{transform:translateX(4px);color:#5664df}.spui-footer{display:flex;justify-content:space-between;gap:12px;margin-top:14px;padding-top:13px;border-top:1px solid #edf0f4;color:#8791a0;font-size:.62rem}.spui-footer span:last-child{color:#5d6bde;font-weight:800}@keyframes spuiEnter{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:none}}@keyframes spuiBar{from{width:0}to{width:var(--spui-target,0%)}}@keyframes spuiPulse{0%,100%{opacity:.72;transform:scale(1)}50%{opacity:1;transform:scale(1.035)}}@media(max-width:780px){.spui-panel{padding:17px;border-radius:20px}.spui-grid{grid-template-columns:1fr}.spui-head h2{font-size:1.35rem}.spui-footer{flex-direction:column;gap:4px}}@media(prefers-reduced-motion:reduce){.spui-panel,.spui-live,.spui-bar i{animation:none!important}.spui-card{transition:none!important}}
 `;document.head.appendChild(style);}
function render(){if(location.search&&!location.search.includes('page=home'))return;const dashboard=document.querySelector('.app-shell .dashboard');const offering=document.querySelector('.app-shell .offering-grid');if(!dashboard||!offering)return;installStyles();let panel=document.getElementById('class9-subject-progress');if(!panel){panel=document.createElement('section');panel.id='class9-subject-progress';panel.className='spui-panel';offering.insertAdjacentElement('afterend',panel)}const subjects=getSubjectProgress();const html=`<div class="spui-head"><div><span class="spui-eyebrow">YOUR PREPARATION</span><h2>विषयवार प्रगति</h2><p>हर विषय में सीखने, अभ्यास, चुनौती और टेस्ट की प्रगति एक जगह देखें।</p></div><span class="spui-live">● LIVE</span></div><div class="spui-grid">${SUBJECTS.map(subject=>{const row=subjects[subject.id]||{...subject,percent:0,chapterStarted:0,fullyCompleted:0};return `<button type="button" class="spui-card" data-subject="${subject.id}" aria-label="${subject.name} progress ${row.percent}%"><span class="spui-icon">${subject.icon}</span><span class="spui-main"><span class="spui-title"><strong>${subject.name}</strong><b>${row.percent}%</b></span><span class="spui-bar"><i style="--spui-target:${row.percent}%;width:${row.percent}%"></i></span><span class="spui-meta"><span>${row.chapterStarted}/${subject.total} topics started</span><em>${row.fullyCompleted} complete</em></span></span><span class="spui-arrow">→</span></button>`}).join('')}</div><div class="spui-footer"><span>📊 Tracked from your learning activity</span><span>Ready for Supabase sync</span></div>`;if(panel.innerHTML!==html)panel.innerHTML=html;panel.querySelectorAll('[data-subject]').forEach(button=>button.onclick=()=>{const id=button.getAttribute('data-subject');history.pushState({},'',`${location.pathname}?page=subject&subject=${encodeURIComponent(id)}`);window.dispatchEvent(new PopStateEvent('popstate'))});panel.querySelectorAll('.spui-bar i').forEach((el,index)=>el.style.setProperty('--spui-delay',`${index*60}ms`));}
function refresh(){captureExistingSources();render();}
let scheduled=false;function scheduleRefresh(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;refresh()})}
function install(){refresh();const observer=new MutationObserver(scheduleRefresh);observer.observe(document.body,{childList:true,subtree:true});const nativeSetItem=Storage.prototype.setItem;if(!Storage.prototype.__class9ProgressPatched){Storage.prototype.setItem=function(key,value){const result=nativeSetItem.apply(this,arguments);if(['class9-progress','class9-sessions','class9-learning-progress','class9-hindi-chapter-progress-v1'].includes(String(key)))setTimeout(scheduleRefresh,0);return result};Storage.prototype.__class9ProgressPatched=true}window.addEventListener('class9-progress-updated',scheduleRefresh);window.addEventListener('hindi-progress-updated',scheduleRefresh);window.addEventListener('storage',event=>{if(['class9-progress','class9-sessions','class9-learning-progress','class9-hindi-chapter-progress-v1'].includes(event.key))scheduleRefresh()})}
if(typeof window!=='undefined'){try{captureExistingSources()}catch{};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install()}
export {SUBJECTS,MODEL_KEY,EVENTS_KEY};
