import React,{useEffect,useRef,useState} from 'react';
import {GEOGRAPHY_CHAPTER_8} from './geographyChapter8Data';
import {GEOGRAPHY_SUBJECTIVE_CHAPTER_8} from './geographyChapter8SubjectiveData';
import './geographyChapter1.css';

const map='https://commons.wikimedia.org/wiki/Special:Redirect/file/India_outline.svg';
const mapFallback='https://commons.wikimedia.org/wiki/Special:Redirect/file/India%20map%20blank.svg';
const progressKey='sst-geography-ch8-progress';
const completedKey='sst-completed-chapters';
const QUIZ_TIMES={practice:15*60,challenge:20*60,test:30*60};
const formatTime=s=>`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;
const readProgress=()=>{try{return JSON.parse(localStorage.getItem(progressKey)||'{}')||{}}catch{return {}}};
const saveProgress=value=>{try{localStorage.setItem(progressKey,JSON.stringify(value));window.dispatchEvent(new Event('sst-progress-updated'))}catch{}};
const markComplete=()=>{try{const ids=JSON.parse(localStorage.getItem(completedKey)||'[]')||[];if(!ids.includes('geography-ch-8'))localStorage.setItem(completedKey,JSON.stringify([...ids,'geography-ch-8']));window.dispatchEvent(new Event('sst-progress-updated'))}catch{}};
const visibleQuestion=(item,index)=>{const desired=index%4,original=item[2],shift=(original-desired+4)%4,options=item[1].map((_,n)=>item[1][(n+shift)%4]);return {question:item[0],options,correct:desired,explanation:item[3]};};
const MODES=[['learn','🗺️','Learn','मानचित्र के घटक, मापनी, दिशा, संकेत और निर्देशांक समझें।'],['practice','📝','Practice','मानचित्र-पठन, मापनी और दिशा पर अभ्यास।'],['challenge','🔥','Challenge','मापनी गणना, निर्देशांक और मानचित्र-तर्क पर अनुप्रयोग।'],['test','🎯','Final Test','पूरे अध्याय की अंतिम परीक्षा।'],['subjective','✍️','Subjective','लघु, दीर्घ और चैलेंजर लिखित अभ्यास।']];
const modeTimeLabel=mode=>mode==='practice'?'15 मिनट कुल':mode==='challenge'?'20 मिनट कुल':mode==='test'?'30 मिनट कुल':'';

function Learn({complete}){const d=GEOGRAPHY_CHAPTER_8,[i,setI]=useState(0),[terms,setTerms]=useState(false),c=d.lessons[i];return <div className="geo-learn"><div className="geo-lesson-nav">{d.lessons.map((x,n)=><button type="button" key={x.title} className={n===i?'is-active':''} onClick={()=>setI(n)}>{String(n+1).padStart(2,'0')}<span>{x.title}</span></button>)}</div><article className="geo-lesson-card"><div className="geo-kicker">LESSON {i+1} / {d.lessons.length}</div><h2>{c.title}</h2><p className="geo-summary">{c.summary}</p><div className="geo-point-grid">{c.points.map(p=><div className="geo-point" key={p}><span>✓</span><p>{p}</p></div>)}</div>{i===1&&<figure className="geo-map-card"><img src={map} alt={d.visual.alt} loading="lazy" referrerPolicy="no-referrer" onError={e=>{if(e.currentTarget.dataset.fallback)return;e.currentTarget.dataset.fallback='1';e.currentTarget.src=mapFallback}}/><figcaption>अध्ययन-संदर्भ: Wikimedia Commons की India outline map. मानचित्र में सीमाओं/रेखाओं की व्याख्या करते समय स्रोत की legend और उपयोग-शर्तें देखें।</figcaption></figure>}<div className="geo-progress"><span style={{width:((i+1)/d.lessons.length)*100+'%'}}/></div><div className="geo-actions"><button type="button" className="geo-secondary" disabled={!i} onClick={()=>setI(v=>v-1)}>← पिछला</button>{i===d.lessons.length-1?<button type="button" className="geo-primary" onClick={()=>complete('learn',d.lessons.length,d.lessons.length)}>✓ Learn पूरा करें</button>:<button type="button" className="geo-primary" onClick={()=>setI(v=>v+1)}>अगला →</button>}</div></article><section className="geo-timeline-card"><div className="geo-section-head"><div><span>MAP SKILLS</span><h3>अध्याय की रीढ़</h3></div><small>मापनी + दिशा + संकेत</small></div><div className="geo-timeline">{d.timeline.map((x,n)=><div className="geo-timeline-item" key={n}><div className="geo-dot">{n+1}</div><div><strong>{x[0]} · {x[1]}</strong><p>{x[2]}</p></div></div>)}</div></section><section className="geo-terms-card"><button type="button" onClick={()=>setTerms(v=>!v)}><span>KEY TERMS</span><strong>मुख्य शब्दावली</strong><b>{terms?'−':'+'}</b></button>{terms&&<div className="geo-term-grid">{d.keyTerms.map(x=><div key={x[0]}><strong>{x[0]}</strong><p>{x[1]}</p></div>)}</div>}</section></div>}

function Quiz({mode,complete}){const d=GEOGRAPHY_CHAPTER_8,q=mode==='test'?d.finalTest:d[mode]||[],duration=QUIZ_TIMES[mode]||15*60;const [i,setI]=useState(0),[answers,setAnswers]=useState([]),[done,setDone]=useState(false),[timeExpired,setTimeExpired]=useState(false),[timeLeft,setTimeLeft]=useState(duration);const completedRef=useRef(false);useEffect(()=>{setI(0);setAnswers([]);setDone(false);setTimeExpired(false);setTimeLeft(duration);completedRef.current=false},[mode,duration]);useEffect(()=>{if(done||timeExpired||!q.length)return;if(timeLeft<=0){setTimeExpired(true);return}const timer=setTimeout(()=>setTimeLeft(v=>v-1),1000);return()=>clearTimeout(timer)},[timeLeft,done,timeExpired,q.length]);const review=q.map((item,n)=>{const x=visibleQuestion(item,n),selected=answers[n];return {n,question:x.question,options:x.options,correct:x.correct,selected,correctAnswer:x.options[x.correct],yourAnswer:selected===null||selected===undefined?null:x.options[selected],explanation:x.explanation,marks:selected===x.correct?1:0};});const score=review.reduce((sum,x)=>sum+x.marks,0);const saveCompletion=()=>{if(completedRef.current)return;completedRef.current=true;complete(mode,score,q.length)};useEffect(()=>{if(timeExpired&&!done){setDone(true);saveCompletion()}},[timeExpired,done,score]);if(!q.length)return <div className="geo-result-card"><h2>इस चरण का प्रश्न बैंक उपलब्ध नहीं है।</h2></div>;const finish=()=>{setDone(true);setTimeExpired(false);saveCompletion()};if(done)return <div className="geo-review-card"><div className="geo-review-summary"><div className="geo-result-ring">{score}<small>/{q.length}</small></div><div><span className="geo-result-label">{mode==='practice'?'PRACTICE REVIEW':mode==='challenge'?'CHALLENGE REVIEW':'FINAL TEST REVIEW'}</span><h2>{timeExpired?'⏰ समय समाप्त — परीक्षा समाप्त':'परीक्षा पूर्ण'}</h2><p>कुल अंक: <strong>{score} / {q.length}</strong> • सही: <strong>{score}</strong> • गलत/अनुत्तरित: <strong>{q.length-score}</strong></p></div></div><div className="geo-review-list">{review.map(r=><article className={'geo-review-item '+(r.selected===null||r.selected===undefined?'unanswered':r.marks?'right':'wrong')} key={r.n}><div className="geo-review-item-top"><strong>प्रश्न {r.n+1}</strong><span>{r.marks}/1 अंक</span></div><h3>{r.question}</h3><p><b>आपका उत्तर:</b> {r.yourAnswer??'अनुत्तरित'}</p><p><b>सही उत्तर:</b> {r.correctAnswer}</p><p className="geo-review-explain">{r.explanation}</p></article>)}</div><div className="geo-review-actions"><button type="button" className="geo-primary" onClick={()=>{setI(0);setAnswers([]);setDone(false);setTimeExpired(false);setTimeLeft(duration);completedRef.current=false}}>फिर से प्रयास करें</button></div></div>;const x=visibleQuestion(q[i],i),selected=answers[i],locked=selected!==undefined&&selected!==null;const choose=n=>{if(locked||timeExpired)return;setAnswers(prev=>{const next=[...prev];next[i]=n;return next});};const next=()=>{if(i<q.length-1)setI(v=>v+1);else finish()};const timerClass=timeLeft<=60?'danger':timeLeft<=300?'warning':'';return <div className="geo-quiz-card"><div className="geo-quiz-top"><span>{mode==='practice'?'PRACTICE':mode==='challenge'?'CHALLENGE':'FINAL TEST'}</span><div><strong>{i+1} / {q.length}</strong><span className={`geo-timer ${timerClass}`} role="timer" aria-live="polite">⏱ {formatTime(timeLeft)}</span></div></div><div className="geo-quiz-track"><span style={{width:((i+1)/q.length)*100+'%'}}/></div><p className="geo-timer-note">पूरे {mode==='practice'?'Practice':mode==='challenge'?'Challenge':'Final Test'} के लिए कुल समय: <strong>{formatTime(duration)}</strong> • प्रश्न बदलने पर timer reset नहीं होगा • उत्तर अंत में review होगा।</p><h2>{x.question}</h2><div className="geo-options">{x.options.map((o,n)=><button type="button" key={o} disabled={locked||timeExpired} className={selected===n?'selected':''} onClick={()=>choose(n)}><span>{String.fromCharCode(65+n)}</span>{o}</button>)}</div>{selected!==undefined&&selected!==null&&<div className="geo-selection-note">✓ उत्तर दर्ज हो गया है • सही/गलत परिणाम परीक्षा के अंत में दिखेगा।</div>}<div className="geo-quiz-footer"><small>{selected!==undefined&&selected!==null?'उत्तर सुरक्षित है':'एक विकल्प चुनें'}</small><button type="button" className="geo-primary" onClick={next}>{i===q.length-1?'परीक्षा समाप्त करें':'अगला प्रश्न →'}</button></div></div>}

function Subjective(){const d=GEOGRAPHY_SUBJECTIVE_CHAPTER_8,l=[['easy','🌱 आसान','मूल तथ्य और पहचान'],['hard','🧠 कठिन','अंतर, कारण और व्याख्या'],['challenger','🏆 चैलेंजर','गणना, तर्क और मानचित्र-विश्लेषण']];return <section className="geo-subjective"><div className="geo-subjective-head"><span>✍ SUBJECTIVE PRACTICE</span><h2>विषयपरक प्रश्न</h2><p>{d.title} • 15 प्रश्न • उत्तर अपने शब्दों में लिखें</p></div><div className="geo-subjective-levels">{l.map(([k,label,desc])=><div className={'geo-subjective-level level-'+k} key={k}><div className="geo-level-head"><div><span>{label}</span><strong>{k==='easy'?'आसान':k==='hard'?'कठिन':'चैलेंजर'}</strong></div><small>{d.questions[k].length} प्रश्न</small></div><p className="geo-level-desc">{desc}</p><div className="geo-subjective-list">{d.questions[k].map((x,n)=><article className="geo-subjective-item" key={n}><div className="geo-number">{String(n+1).padStart(2,'0')}</div><div><p>{x.q}</p><span>{x.marks} अंक</span></div></article>)}</div></div>)}</div></section>}

export function GeographyChapter8EngineClean({onBack}){const d=GEOGRAPHY_CHAPTER_8,[mode,setMode]=useState(null),[progress,setProgress]=useState(readProgress);const complete=(id,score,total)=>{const n={...progress,[id]:{score,total,completedAt:new Date().toISOString()}};if(id==='test'){n.chapterCompleted={completedAt:new Date().toISOString(),score,total};markComplete()}setProgress(n);saveProgress(n)};return <main className="geo-chapter"><header className="geo-header"><button type="button" className="geo-back" onClick={onBack}>← सामाजिक विज्ञान</button><div><span>भूगोल • अध्याय 8 • बिहार बोर्ड</span><h1>{d.title}</h1><p>{d.subtitle}</p>{progress.chapterCompleted&&<div className="geo-complete">✓ CHAPTER COMPLETED</div>}</div><div className="geo-stat"><strong>{['learn','practice','challenge','test'].filter(x=>progress[x]).length}/4</strong><span>stages complete</span></div></header><section className="geo-content"><div className="geo-intro"><div><span>अध्याय का लक्ष्य</span><p>{d.goal}</p></div><div className="geo-chips"><span>{d.lessons.length} lessons</span><span>{d.practice.length} practice</span><span>{d.challenge.length} challenge</span><span>{d.finalTest.length} test</span><span>15 subjective</span></div></div>{!mode?<div className="geo-mode-grid">{MODES.map(([id,icon,title,desc])=><button type="button" key={id} className={'geo-mode-card '+(id!=='subjective'&&progress[id]?'completed':'')} onClick={()=>setMode(id)}><span className="geo-mode-icon">{icon}</span><strong>{title}</strong><p>{desc}</p>{id!=='subjective'&&<em>{progress[id]?'✓ '+progress[id].score+'/'+progress[id].total:'⏱ '+modeTimeLabel(id)}</em>}{id==='subjective'&&<em>शुरू करें →</em>}</button>)}</div>:<div className="geo-workspace"><button type="button" className="geo-mode-back" onClick={()=>setMode(null)}>← stages पर वापस</button>{mode==='learn'?<Learn complete={complete}/>:mode==='subjective'?<Subjective/>:<Quiz mode={mode} complete={complete}/>}</div>}</section></main>}
