import React,{useEffect,useMemo,useRef,useState} from 'react';
import {HISTORY_CHAPTER_5} from './historyChapter5Data';
import {SubjectiveQuestionsCard} from './SubjectiveQuestionsCard';
import './historyChapter.css';
const storageKey='sst-history-ch5-progress';
const readProgress=()=>{try{return JSON.parse(localStorage.getItem(storageKey)||'{}')||{}}catch{return {}}};
const saveProgress=v=>{try{localStorage.setItem(storageKey,JSON.stringify(v));window.dispatchEvent(new Event('sst-progress-updated'))}catch{}};
const markComplete=()=>{try{const ids=JSON.parse(localStorage.getItem('sst-completed-chapters')||'[]')||[];if(!ids.includes('history-ch-5'))localStorage.setItem('sst-completed-chapters',JSON.stringify([...ids,'history-ch-5']));window.dispatchEvent(new Event('sst-progress-updated'))}catch{}};
const secondsPerQuestion=mode=>mode==='practice'?40:60;const formatTime=s=>`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;
const MODES=[{id:'learn',icon:'📖',title:'Learn',desc:'पाठ को क्रम से समझें और मुख्य बिंदु दोहराएँ।'},{id:'practice',icon:'📝',title:'Practice',desc:'अध्याय की मूल अवधारणाओं पर अभ्यास करें।'},{id:'challenge',icon:'🔥',title:'Challenge',desc:'कारण–परिणाम और समझ आधारित प्रश्न।'},{id:'test',icon:'🎯',title:'Final Test',desc:'पूरे अध्याय की अंतिम परीक्षा।'},{id:'subjective',icon:'✍️',title:'Subjective',desc:'लिखित उत्तर और बोर्ड-शैली अभ्यास।'}];
function LearnView({onComplete}){const data=HISTORY_CHAPTER_5;const [lesson,setLesson]=useState(0),[showTerms,setShowTerms]=useState(false);const current=data.lessons[lesson];return <div className="history-learn"><div className="history-learn-nav" role="tablist" aria-label="अध्याय के पाठ">{data.lessons.map((item,i)=><button key={item.title} className={i===lesson?'is-active':''} onClick={()=>setLesson(i)}>{String(i+1).padStart(2,'0')}<span>{item.title}</span></button>)}</div><article className="history-lesson-card"><div className="history-lesson-kicker">LESSON {lesson+1} / {data.lessons.length}</div><h2>{current.title}</h2><p className="history-lesson-summary">{current.summary}</p><div className="history-point-grid">{current.points.map(point=><div key={point} className="history-point"><span>✓</span><p>{point}</p></div>)}</div><div className="history-progress-bar"><span style={{width:`${((lesson+1)/data.lessons.length)*100}%`}}/></div><div className="history-lesson-actions"><button className="history-secondary" disabled={lesson===0} onClick={()=>setLesson(v=>Math.max(0,v-1))}>← पिछला</button>{lesson===data.lessons.length-1?<button className="history-primary" onClick={()=>onComplete('learn',data.lessons.length,data.lessons.length)}>✓ Learn पूरा करें</button>:<button className="history-primary" onClick={()=>setLesson(v=>v+1)}>अगला →</button>}</div></article><section className="history-timeline-card"><div className="history-section-head"><div><span>QUICK TIMELINE</span><h3>मुख्य घटनाएँ एक नज़र में</h3></div><small>कालक्रम याद रखें</small></div><div className="history-timeline">{data.timeline.map((item,i)=><div className="history-timeline-item" key={item[0]}><div className="history-timeline-dot">{i+1}</div><div><strong>{item[0]} · {item[1]}</strong><p>{item[2]}</p></div></div>)}</div></section><section className="history-terms-card"><button onClick={()=>setShowTerms(v=>!v)}><span>KEY TERMS</span><strong>मुख्य शब्दावली</strong><b>{showTerms?'−':'+'}</b></button>{showTerms&&<div className="history-term-grid">{data.keyTerms.map(([term,meaning])=><div key={term}><strong>{term}</strong><p>{meaning}</p></div>)}</div>}</section></div>}
function QuizView({mode,onComplete}){const data=HISTORY_CHAPTER_5;const questions=mode==='test'?data.finalTest:data[mode]||[];const [index,setIndex]=useState(0),[picked,setPicked]=useState(null),[score,setScore]=useState(0),[done,setDone]=useState(false),[timeLeft,setTimeLeft]=useState(()=>questions.length*secondsPerQuestion(mode));const scoreRef=useRef(0),pickedRef=useRef(null),doneRef=useRef(false),completeRef=useRef(onComplete);useEffect(()=>{scoreRef.current=score},[score]);useEffect(()=>{pickedRef.current=picked},[picked]);useEffect(()=>{doneRef.current=done},[done]);useEffect(()=>{completeRef.current=onComplete},[onComplete]);useEffect(()=>{setIndex(0);setPicked(null);setScore(0);setDone(false);doneRef.current=false;setTimeLeft(questions.length*secondsPerQuestion(mode))},[mode,questions.length]);const finish=()=>{if(doneRef.current)return;const finalScore=scoreRef.current+(pickedRef.current===questions[index]?.[2]?1:0);doneRef.current=true;setScore(finalScore);setDone(true);setTimeLeft(0);completeRef.current(mode,finalScore,questions.length)};useEffect(()=>{if(done||!questions.length)return;const timer=setInterval(()=>setTimeLeft(v=>{if(v<=1){clearInterval(timer);finish();return 0}return v-1}),1000);return()=>clearInterval(timer)},[done,index,questions.length,mode]);const q=questions[index];if(!q)return <div className="history-result-card"><h2>इस चरण का प्रश्न बैंक उपलब्ध नहीं है।</h2></div>;if(done)return <div className="history-result-card"><div className="history-result-ring">{score}<small>/{questions.length}</small></div><span className="history-result-label">{mode==='practice'?'PRACTICE COMPLETE':mode==='challenge'?'CHALLENGE COMPLETE':'FINAL TEST COMPLETE'}</span><h2>{score/questions.length>=.8?'बहुत बढ़िया!':score/questions.length>=.6?'अच्छी तैयारी!':'एक बार फिर दोहराएँ।'}</h2><p>आपका स्कोर {score} / {questions.length} है।</p>{mode==='test'&&<div className="history-chapter-finish">✓ अध्याय पूरा हुआ — अब यह chapter completed के रूप में सेव है।</div>}<button className="history-primary" onClick={()=>{setIndex(0);setPicked(null);setScore(0);setDone(false);doneRef.current=false;setTimeLeft(questions.length*secondsPerQuestion(mode))}}>फिर से प्रयास करें</button></div>;const answered=picked!==null,totalSeconds=questions.length*secondsPerQuestion(mode),timerClass=timeLeft<=Math.max(10,Math.floor(totalSeconds*.25))?'danger':timeLeft<=Math.floor(totalSeconds*.5)?'warning':'';return <div className="history-quiz-card"><div className="history-quiz-top"><span>{mode==='practice'?'PRACTICE':mode==='challenge'?'CHALLENGE':'FINAL TEST'}</span><div className="history-quiz-meta"><strong>{index+1} / {questions.length}</strong><span className={`history-timer ${timerClass}`}>⏱ {formatTime(timeLeft)}</span></div></div><div className="history-quiz-track"><span style={{width:`${((index+1)/questions.length)*100}%`}}/></div><p className="history-timer-note">हर प्रश्न के लिए {secondsPerQuestion(mode)} सेकंड • कुल समय {formatTime(totalSeconds)}</p><h2>{q[0]}</h2><div className="history-options">{q[1].map((option,choice)=><button key={option} className={answered?(choice===q[2]?'correct':choice===picked?'wrong':''):''} disabled={answered} onClick={()=>{if(!answered)setPicked(choice)}}><span>{String.fromCharCode(65+choice)}</span>{option}</button>)}</div>{answered&&<div className={`history-explain ${picked===q[2]?'ok':'no'}`}><strong>{picked===q[2]?'✓ सही उत्तर':'✕ ध्यान दें'}</strong><p>{q[3]}</p></div>}<div className="history-quiz-footer"><small>{answered?'उत्तर lock हो गया है':'एक विकल्प चुनें'}</small><button className="history-primary" disabled={!answered} onClick={()=>{const nextScore=score+(picked===q[2]?1:0);if(index<questions.length-1){setScore(nextScore);setIndex(v=>v+1);setPicked(null)}else{setScore(nextScore);doneRef.current=true;setDone(true);completeRef.current(mode,nextScore,questions.length)}}}>{index===questions.length-1?'परिणाम देखें':'अगला प्रश्न →'}</button></div></div>}
export function HistoryChapter5Engine({onBack}){const data=HISTORY_CHAPTER_5;const [mode,setMode]=useState(null),[progress,setProgress]=useState(readProgress);useEffect(()=>setProgress(readProgress()),[]);const completion=['learn','practice','challenge','test'].filter(id=>progress[id]).length;const complete=(id,score,total)=>{const next={...progress,[id]:{score,total,completedAt:new Date().toISOString()}};if(id==='test'){next.chapterCompleted={completedAt:new Date().toISOString(),score,total};markComplete()}setProgress(next);saveProgress(next)};return <main className="history-chapter"><header className="history-chapter-header"><button type="button" className="history-back" onClick={onBack}>← सामाजिक विज्ञान</button><div className="history-header-copy"><span>इतिहास • अध्याय 5 • बिहार बोर्ड</span><h1>{data.title}</h1><p>{data.subtitle}</p>{progress.chapterCompleted&&<div className="history-complete-badge">✓ CHAPTER COMPLETED</div>}</div><div className="history-stat-card"><strong>{completion}/4</strong><span>stages complete</span></div></header><section className="history-content"><div className="history-chapter-intro"><div><span>अध्याय का लक्ष्य</span><p>{data.goal}</p></div><div className="history-chip-row"><span>{data.lessons.length} lessons</span><span>{data.practice.length} practice</span><span>{data.challenge.length} challenge</span><span>{data.finalTest.length} test</span><span>12 subjective</span></div></div>{!mode?<div className="history-mode-grid">{MODES.map(item=><button key={item.id} className={`history-mode-card ${item.id!=='subjective'&&progress[item.id]?'completed':''}`} onClick={()=>setMode(item.id)}><span className="history-mode-icon">{item.icon}</span><strong>{item.title}</strong><p>{item.desc}</p><em>{item.id!=='subjective'&&progress[item.id]?`✓ ${progress[item.id].score}/${progress[item.id].total}`:'शुरू करें →'}</em></button>)}</div>:<div className="history-workspace"><button className="history-mode-back" onClick={()=>setMode(null)}>← stages पर वापस</button>{mode==='learn'?<LearnView onComplete={complete}/>:mode==='subjective'?<SubjectiveQuestionsCard chapterNumber={5}/>:<QuizView mode={mode} onComplete={complete}/>}</div>}</section></main>}
