import React,{useEffect,useMemo,useRef,useState} from 'react';
import {GEOGRAPHY_CHAPTER_10} from './geographyChapter10Data';
import {GEOGRAPHY_SUBJECTIVE_CHAPTER_10} from './geographyChapter10SubjectiveData';
import './geographyChapter1.css';

const progressKey='sst-geography-ch10-progress';
const completedKey='sst-completed-chapters';
const QUIZ_TIMES={practice:15*60,challenge:20*60,test:30*60};
const MODES=[
 ['learn','📘','Learn','आपदा, जोखिम, तैयारी, राहत, पुनर्वास और न्यूनन की समझ।'],
 ['practice','📝','Practice','परिभाषा, वर्गीकरण और मूल आपदा-प्रबंधन अवधारणाओं का अभ्यास।'],
 ['challenge','🔥','Challenge','स्थिति-आधारित तर्क, योजना और जोखिम-विश्लेषण।'],
 ['test','🎯','Final Test','पूरे अध्याय का समयबद्ध अंतिम मूल्यांकन।'],
 ['subjective','✍️','Subjective','आसान, कठिन और चैलेंजर वर्णनात्मक प्रश्न।']
];
const modeTimeLabel=m=>m==='practice'?'15 मिनट कुल':m==='challenge'?'20 मिनट कुल':m==='test'?'30 मिनट कुल':'';
const formatTime=s=>`${String(Math.floor(Math.max(0,s)/60)).padStart(2,'0')}:${String(Math.max(0,s)%60).padStart(2,'0')}`;
const readProgress=()=>{try{return JSON.parse(localStorage.getItem(progressKey)||'{}')||{}}catch{return {}}};
const saveProgress=value=>{try{localStorage.setItem(progressKey,JSON.stringify(value));window.dispatchEvent(new Event('sst-progress-updated'))}catch{}};
const markComplete=()=>{try{const ids=JSON.parse(localStorage.getItem(completedKey)||'[]')||[];if(!ids.includes('geography-ch-10'))localStorage.setItem(completedKey,JSON.stringify([...ids,'geography-ch-10']));window.dispatchEvent(new Event('sst-progress-updated'))}catch{}};
const visibleQuestion=(item,index)=>{const options=item.o||[];const n=options.length;if(!n)return {question:item.q,options:[],correct:0,explanation:item.e||''};const original=Number.isInteger(item.a)?item.a:0;const desired=index%n;const shift=((original-desired)%n+n)%n;const rotated=options.map((_,i)=>options[(i+shift)%n]);return {question:item.q,options:rotated,correct:desired,explanation:item.e||''};};

function Learn({complete}){const d=GEOGRAPHY_CHAPTER_10,[i,setI]=useState(0),[terms,setTerms]=useState(false),c=d.lessons[i];return <div className="geo-learn"><div className="geo-lesson-nav">{d.lessons.map((x,n)=><button type="button" key={x.title} className={n===i?'is-active':''} onClick={()=>setI(n)}>{String(n+1).padStart(2,'0')}<span>{x.title.replace(/^\d+\.\s*/,'')}</span></button>)}</div><article className="geo-lesson-card"><div className="geo-kicker">LESSON {i+1} / {d.lessons.length}</div><h2>{c.title}</h2><p className="geo-summary">{c.summary}</p><div className="geo-point-grid">{c.points.map(p=><div className="geo-point" key={p}><span>✓</span><p>{p}</p></div>)}</div><div className="geo-progress"><span style={{width:((i+1)/d.lessons.length)*100+'%'}}/></div><div className="geo-actions"><button type="button" className="geo-secondary" disabled={!i} onClick={()=>setI(v=>v-1)}>← पिछला</button>{i===d.lessons.length-1?<button type="button" className="geo-primary" onClick={()=>complete('learn',d.lessons.length,d.lessons.length)}>✓ Learn पूरा करें</button>:<button type="button" className="geo-primary" onClick={()=>setI(v=>v+1)}>अगला →</button>}</div></article><section className="geo-timeline-card"><div className="geo-section-head"><div><span>DISASTER MANAGEMENT FLOW</span><h3>अध्याय की कार्य-श्रृंखला</h3></div><small>जोखिम → तैयारी → प्रतिक्रिया → पुनर्वास → न्यूनन</small></div><div className="geo-timeline">{d.timeline.map((x,n)=><div className="geo-timeline-item" key={x.label}><div className="geo-dot">{n+1}</div><div><strong>{x.label} · {x.value}</strong><p>{x.detail}</p></div></div>)}</div></section><section className="geo-terms-card"><button type="button" onClick={()=>setTerms(v=>!v)}><span>KEY TERMS</span><strong>मुख्य शब्दावली</strong><b>{terms?'−':'+'}</b></button>{terms&&<div className="geo-term-grid">{d.keyTerms.map(x=><div key={x[0]}><strong>{x[0]}</strong><p>{x[1]}</p></div>)}</div>}</section></div>}

function Quiz({mode,complete,onBack}){const d=GEOGRAPHY_CHAPTER_10,q=mode==='test'?d.finalTest:d[mode]||[],duration=QUIZ_TIMES[mode]||15*60;const [i,setI]=useState(0),[answers,setAnswers]=useState([]),[done,setDone]=useState(false),[timeExpired,setTimeExpired]=useState(false),[timeLeft,setTimeLeft]=useState(duration);const completedRef=useRef(false);useEffect(()=>{setI(0);setAnswers([]);setDone(false);setTimeExpired(false);setTimeLeft(duration);completedRef.current=false},[mode,duration]);useEffect(()=>{if(done||timeExpired||!q.length)return;if(timeLeft<=0){setTimeExpired(true);return}const id=window.setTimeout(()=>setTimeLeft(v=>v-1),1000);return()=>window.clearTimeout(id)},[timeLeft,done,timeExpired,q.length]);const review=useMemo(()=>q.map((item,n)=>{const x=visibleQuestion(item,n),selected=answers[n];return {n,question:x.question,correct:x.correct,selected,yourAnswer:selected===null||selected===undefined?null:x.options[selected],correctAnswer:x.options[x.correct],explanation:x.explanation,marks:selected===x.correct?1:0}}),[q,answers]);const score=review.reduce((sum,x)=>sum+x.marks,0);const saveCompletion=()=>{if(completedRef.current)return;completedRef.current=true;complete(mode,score,q.length)};useEffect(()=>{if(timeExpired&&!done){setDone(true);saveCompletion()}},[timeExpired,done,score]);if(!q.length)return <div className="geo-result-card"><h2>इस चरण का प्रश्न बैंक उपलब्ध नहीं है।</h2></div>;if(done)return <div className="geo-review-card"><div className="geo-review-summary"><div className="geo-result-ring">{score}<small>/{q.length}</small></div><div><span className="geo-result-label">{mode==='practice'?'PRACTICE REVIEW':mode==='challenge'?'CHALLENGE REVIEW':'FINAL TEST REVIEW'}</span><h2>{timeExpired?'⏰ समय समाप्त — परीक्षा समाप्त':'परीक्षा पूर्ण'}</h2><p>कुल अंक: <strong>{score} / {q.length}</strong> • सही: <strong>{score}</strong> • गलत/अनुत्तरित: <strong>{q.length-score}</strong></p></div></div><div className="geo-review-list">{review.map(r=><article className={'geo-review-item '+(r.selected===null||r.selected===undefined?'unanswered':r.marks?'right':'wrong')} key={r.n}><div className="geo-review-item-top"><strong>प्रश्न {r.n+1}</strong><span>{r.marks}/1 अंक</span></div><h3>{r.question}</h3><p><b>आपका उत्तर:</b> {r.yourAnswer??'अनुत्तरित'}</p><p><b>सही उत्तर:</b> {r.correctAnswer}</p><p className="geo-review-explain">व्याख्या: {r.explanation}</p></article>)}</div><div className="geo-review-actions"><button type="button" className="geo-primary" onClick={()=>{setI(0);setAnswers([]);setDone(false);setTimeExpired(false);setTimeLeft(duration);completedRef.current=false}}>फिर से प्रयास करें</button><button type="button" className="geo-secondary" onClick={onBack}>चरण चयन</button></div></div>;const x=visibleQuestion(q[i],i),selected=answers[i],locked=selected!==undefined&&selected!==null;const choose=n=>{if(locked||timeExpired)return;setAnswers(prev=>{const next=[...prev];next[i]=n;return next})};const next=()=>{if(i<q.length-1)setI(v=>v+1);else finish()};const finish=()=>{setDone(true);setTimeExpired(false);saveCompletion()};const timerClass=timeLeft<=60?'danger':timeLeft<=300?'warning':'';return <div className="geo-quiz-card"><div className="geo-quiz-top"><span>{mode==='practice'?'PRACTICE':mode==='challenge'?'CHALLENGE':'FINAL TEST'}</span><div><strong>{i+1} / {q.length}</strong><span className={`geo-timer ${timerClass}`} role="timer" aria-live="polite">⏱ {formatTime(timeLeft)}</span></div></div><div className="geo-quiz-track"><span style={{width:((i+1)/q.length)*100+'%'}}/></div><p className="geo-timer-note">पूरे {mode==='practice'?'Practice':mode==='challenge'?'Challenge':'Final Test'} के लिए कुल समय: <strong>{formatTime(duration)}</strong> • प्रश्न बदलने पर timer reset नहीं होगा • परिणाम अंत में दिखेगा।</p><h2>{x.question}</h2><div className="geo-options">{x.options.map((o,n)=><button type="button" key={`${n}-${o}`} disabled={locked||timeExpired} className={selected===n?'selected':''} onClick={()=>choose(n)}><span>{String.fromCharCode(65+n)}</span>{o}</button>)}</div>{locked&&<div className="geo-selection-note">✓ उत्तर दर्ज हो गया है • सही/गलत परिणाम परीक्षा के अंत में दिखेगा।</div>}<div className="geo-quiz-footer"><small>{locked?'उत्तर सुरक्षित है':'एक विकल्प चुनें'}</small><button type="button" className="geo-primary" onClick={next}>{i===q.length-1?'परीक्षा समाप्त करें':'अगला प्रश्न →'}</button></div></div>}

function Subjective(){const d=GEOGRAPHY_SUBJECTIVE_CHAPTER_10.questions,l=[['easy','🌱 आसान','मूल तथ्य और परिभाषाएँ'],['hard','🧠 कठिन','क्रम, कारण और भूमिका'],['challenger','🏆 चैलेंजर','स्थिति-आधारित तर्क और योजना']];return <section className="geo-subjective"><div className="geo-subjective-head"><span>✍ SUBJECTIVE PRACTICE</span><h2>विषयपरक प्रश्न</h2><p>आपदा प्रबंधन : एक परिचय • 15 प्रश्न • उत्तर अपने शब्दों में लिखें</p></div><div className="geo-subjective-levels">{l.map(([k,label,desc])=><div className={'geo-subjective-level level-'+k} key={k}><div className="geo-level-head"><div><span>{label}</span><strong>{k==='easy'?'आसान':k==='hard'?'कठिन':'चैलेंजर'}</strong></div><small>{d[k].length} प्रश्न</small></div><p className="geo-level-desc">{desc}</p><div className="geo-subjective-list">{d[k].map((x,n)=><article className="geo-subjective-item" key={n}><div className="geo-number">{String(n+1).padStart(2,'0')}</div><div><p>{x.q}</p><span>{x.marks} अंक</span><details><summary>मॉडल उत्तर देखें</summary><p><strong>मॉडल उत्तर:</strong> {x.a}</p></details></div></article>)}</div></div>)}</div></section>}

export function GeographyChapter10EngineClean({onBack}){const d=GEOGRAPHY_CHAPTER_10,[mode,setMode]=useState(null),[progress,setProgress]=useState(readProgress);const complete=(id,score,total)=>{const n={...progress,[id]:{score,total,completedAt:new Date().toISOString()}};if(id==='test'){n.chapterCompleted={completedAt:new Date().toISOString(),score,total};markComplete()}setProgress(n);saveProgress(n)};const workspace=mode&&<div className="geo-workspace"><button type="button" className="geo-mode-back" onClick={()=>setMode(null)}>← चरण चयन पर लौटें</button>{mode==='learn'?<Learn complete={complete}/>:mode==='subjective'?<Subjective/>:<Quiz mode={mode} complete={complete} onBack={()=>setMode(null)}/>}</div>;return <main className="geo-chapter"><header className="geo-header"><button type="button" className="geo-back" onClick={onBack}>← सामाजिक विज्ञान</button><div><span>भूगोल • अध्याय 10 • बिहार बोर्ड</span><h1>{d.title}</h1><p>{d.subtitle}</p>{progress.chapterCompleted&&<div className="geo-complete">✓ CHAPTER COMPLETED</div>}</div><div className="geo-stat"><strong>{['learn','practice','challenge','test'].filter(x=>progress[x]).length}/4</strong><span>stages complete</span></div></header><section className="geo-content"><div className="geo-intro"><div><span>अध्याय का लक्ष्य</span><p>{d.goal}</p></div><div className="geo-chips"><span>{d.lessons.length} lessons</span><span>{d.practice.length} practice</span><span>{d.challenge.length} challenge</span><span>{d.finalTest.length} test</span><span>5 easy subjective</span><span>5 hard</span><span>5 challenger</span></div></div>{workspace||<div className="geo-mode-grid">{MODES.map(([id,icon,title,desc])=><button type="button" key={id} className={'geo-mode-card '+(id!=='subjective'&&progress[id]?'completed':'')} onClick={()=>setMode(id)}><span className="geo-mode-icon">{icon}</span><strong>{title}</strong><p>{desc}</p>{id!=='subjective'&&<em>{progress[id]?'✓ '+progress[id].score+'/'+progress[id].total:'⏱ '+modeTimeLabel(id)}</em>}{id==='subjective'&&<em>15 प्रश्न • शुरू करें →</em>}</button>)}</div>}</section></main>}
