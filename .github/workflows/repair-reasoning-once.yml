name: Retired One-Time Reasoning Repair
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired one-time repair helper. Future source changes are explicit."