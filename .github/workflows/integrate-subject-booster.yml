name: Retired Subject Booster Integrator
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy booster auto-patch disabled. Content changes must be explicit and verified."